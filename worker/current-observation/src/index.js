import allowlist from '../data/live-locations.json' with {type:'json'};
import {json,parseUSGSFeatures,freshness} from './core.js';

export default {async fetch(request,env,cfg){const url=new URL(request.url);const match=url.pathname.match(/^\/v1\/current\/([^/]+)$/);if(!match)return json({error:'not found'},404,env.PUBLIC_ORIGIN||'');const id=decodeURIComponent(match[1]);const item=allowlist[id];if(!item)return json({error:'unknown location'},404,env.PUBLIC_ORIGIN||'');const upstream=new URL('https://api.waterdata.usgs.gov/ogcapi/v0/collections/latest-continuous/items');upstream.searchParams.set('f','json');upstream.searchParams.set('monitoring_location_id',`USGS-${item.station_id}`);upstream.searchParams.set('limit','20');if(env.USGS_API_KEY)upstream.searchParams.set('api_key',env.USGS_API_KEY);try{const res=await fetch(upstream,{headers:{'user-agent':'Rivermetry/0.1 (+https://rivermetry.example)'}});if(!res.ok)throw new Error('upstream status');const payload=await res.json();const current=parseUSGSFeatures(payload.features);const state=freshness(current.observed_at);if(state==='unavailable')return json({current_observation:null,update_status:'unavailable'},503,env.PUBLIC_ORIGIN||'');return json({location_id:id,current_observation:{...current,trend:'unknown'},update_status:state},200,env.PUBLIC_ORIGIN||'');}catch(e){return json({location_id:id,current_observation:null,update_status:'unavailable'},503,env.PUBLIC_ORIGIN||'');}}};
